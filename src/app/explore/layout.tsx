import { pageMetadata } from '@/lib/page-metadata';
export const metadata = pageMetadata('Explore Pangasinan', 'Discover community-recommended beaches, heritage sites, food, and hidden gems across Pangasinan.', '/explore');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
