import { pageMetadata } from '@/lib/page-metadata';
export const metadata = pageMetadata('Sovereign Navigation', 'Plan an in-app route to Pangasinan destinations with JuanDerQuest navigation.', '/navigate');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
