import type { MetadataRoute } from 'next';
export default function sitemap(): MetadataRoute.Sitemap { const base = process.env.NEXT_PUBLIC_SITE_URL || 'https://jdq.zernanvash.dev'; const paths = ['', '/explore', '/quests', '/map', '/campaigns', '/shop', '/vote', '/about', '/privacy', '/terms']; return paths.map((path) => ({ url: `${base}${path}`, lastModified: new Date(), changeFrequency: path === '/explore' ? 'daily' : 'weekly', priority: path === '' ? 1 : .8 })); }
