import { pageMetadata } from '@/lib/page-metadata';
export const metadata = pageMetadata('About the Project', 'Learn how JuanDerQuest promotes responsible, community-powered tourism in Pangasinan.', '/about');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
