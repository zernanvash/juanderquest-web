import { pageMetadata } from '@/lib/page-metadata';
export const metadata = pageMetadata('Local Rewards Shop', 'Redeem JuanDerQuest rewards with participating Pangasinan merchants.', '/shop');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
