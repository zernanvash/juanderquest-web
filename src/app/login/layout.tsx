import { pageMetadata } from '@/lib/page-metadata';
export const metadata = pageMetadata('Traveler Login', 'Sign in to save destinations, join quests, and manage your JuanDerQuest profile.', '/login');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
