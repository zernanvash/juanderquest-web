import { pageMetadata } from '@/lib/page-metadata';
export const metadata = pageMetadata('Search Destinations', 'Search Pangasinan destinations, activities, food, and community recommendations.', '/search');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
