import { pageMetadata } from '@/lib/page-metadata';
export const metadata = pageMetadata('Interactive Pangasinan Map', 'Browse destinations and tourism experiences on the sovereign JuanDerQuest map.', '/map');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
