import { pageMetadata } from '@/lib/page-metadata';
export const metadata = pageMetadata('Pangasinan Destinations', 'View and contribute community-reviewed tourism destinations across Pangasinan.', '/spots');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
