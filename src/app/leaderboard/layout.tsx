import { pageMetadata } from '@/lib/page-metadata';
export const metadata = pageMetadata('Traveler Leaderboard', 'See leading JuanDerQuest explorers and community contributors.', '/leaderboard');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
