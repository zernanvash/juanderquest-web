import { pageMetadata } from '@/lib/page-metadata';
export const metadata = pageMetadata('Community Campaigns', 'Join community tourism campaigns, local events, and conservation activities in Pangasinan.', '/campaigns');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
