import { pageMetadata } from '@/lib/page-metadata';
export const metadata = pageMetadata('Submission History', 'Review the status of your JuanDerQuest quest and destination submissions.', '/history');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
